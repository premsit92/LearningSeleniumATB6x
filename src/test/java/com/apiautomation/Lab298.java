package com.apiautomation;

import org.openqa.selenium.edge.EdgeDriver;

public class Lab298 {
    public static void main(String[] args) {
        EdgeDriver driver = new EdgeDriver();
        driver.get("https://sdet.live");
        driver.quit();

        //Selenium Flow
        //1. Create Session ID is created
        //Client - Java Program
        //Server = Webdriver Edge - Command W3C --> Edge Browser
        //2. Session --> Run the command - GET -- GET Method w3c Protocol
        //3. EdgeDriveer (Server) --> Tell the edge Browser
        // open a fresh Edge Browser
        //Open the URL
        //5. Driver - Command (Post)Request


    }
}
